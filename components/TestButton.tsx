'use client';

export default function TestButton() {
    return (
        <div className="fixed bottom-6 right-6 z-[9999] w-16 h-16 bg-red-500 rounded-full flex items-center justify-center text-white font-bold shadow-2xl animate-pulse">
            TEST
        </div>
    );
}
